version: 2.1
orbs:
  node: circleci/node@3.0.0
workflows:
  node-tests:
    jobs:
      #      - node/test

      - run:
          name: lint
          command: npm run lint

      - run:
          name: build
          command: npm run build

      - run:
          name: deploy
          command: npm run deploy
