version: 2.1
orbs:
  node: circleci/node@3.0.0
workflows:
  node-tests:
    jobs:
#      - node/test

      - node/lint

      - node/build

#      - run:
#          name: angular-lint
#          command: npm run lint

#      - run:
#          name: angular-build
#          command: npm run build

#      - run:
#            name: deploy
#            command: npm run deploy
